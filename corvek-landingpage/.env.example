# Custom domain (e.g. corvek.com) or leave empty to use default github.io subdomain
VITE_CUSTOM_DOMAIN=

# Base URL: '/' if using a custom domain, or '/corvek-landing/' if deploying to the default github.io URL
VITE_BASE_URL=/
